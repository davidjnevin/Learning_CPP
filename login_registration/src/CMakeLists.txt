# .cpp files here
set(SOURCES
    main.cpp
    # User.cpp
    # Login.cpp
)

# Executable using the sources
add_executable(LoginApp ${SOURCES})

# Libraries
# target_link_libraries(LoginApp PRIVATE your_library)

